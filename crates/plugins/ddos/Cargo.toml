[package]
name = "ddos"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"] # Important: Generates .dll / .so

[dependencies]
plugin_api = { path = "../../shared/plugin_api" }
log = "0.4"
